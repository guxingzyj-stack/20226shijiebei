This is a minimal engineering sample for P3 pipeline tests, not official production player data.

The files in this directory are intentionally small and synthetic-looking enough to validate CSV parsing, dry-run import counts, feature generation, and GBM zero-weight behavior. They must not be treated as official player data or imported into production without explicit test-only intent.
